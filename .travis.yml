dist: trusty
language: java
jdk: oraclejdk8

services:
  - docker

before_script:
  - docker-compose up -d --file ./src/test/java/com/shop/ItemServiceRest/docker-compose-test.yml

after_script:
  - docker-compose --file ./src/test/java/com/shop/ItemServiceRest/docker-compose-test.yml down